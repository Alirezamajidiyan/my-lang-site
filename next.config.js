/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['uploadkon.ir'], // دامنه‌ای که عکس از آن دانلود شده
  },
};

module.exports = nextConfig;
